const Hero = () => {

    return <div className="hero-container">
        <div className="hero-center">
            <h1>Set up a Hero component <br></br>and add CSS.</h1>
            <p>
                Set up NavLinks in the Navbar. Import subunits from data and iterate over them to render pages on the screen. Set up a NavLink CSS.
            </p>
        </div>
    </div>;
};

export default Hero;
